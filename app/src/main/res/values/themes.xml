<resources>
    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/white</item>
        <item name="colorBackground">?android:colorBackground</item>
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="colorToolbarIconTint">?attr/colorOnPrimary</item>
        <!-- Tools. -->
        <item name="colorToolsPanelBorder">@color/gray_300</item>
        <item name="colorToolsPanelBackground">?attr/colorBackground</item>
        <item name="colorToolsSelectedBorder">@color/peru_html</item>
        <item name="colorToolsIcon">@color/black</item>
        <item name="colorToolsText">@color/black</item>
    </style>

    <!-- Light application theme. -->
    <style name="Theme.Canvas" parent="BaseTheme" />
</resources>